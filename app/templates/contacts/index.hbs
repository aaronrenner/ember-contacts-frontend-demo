<h3>Your {{contactsCount}} contacts</h3>
<ul>
  {{#each}}
    <li>
      {{#link-to 'contact' this }}
        {{lastName}}, {{firstName}}
      {{/link-to}}
    </li>
  {{else}}
    <li>No contacts found.</li>
  {{/each}}
</ul>
